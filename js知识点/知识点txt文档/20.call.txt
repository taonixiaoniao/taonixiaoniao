1、call()
	调用这个函数并修改函数运行时的this指向。
	fun.call(thisArg, arg1, arg2,.....)this.Arg是当前调用函数this的指向对象；arg1, arg2是传递的其他参数。
2、继承
	es6之前没有给我们提供extends继承。我们可以通过构造函数+原型对象模拟实现继承，被称为组合继承。
	借用构造函数继承父类的属性：
	核心原理：通过call()把父类的this指向子类的this，这样就可以实现子类继承父类的属性。